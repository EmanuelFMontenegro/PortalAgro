export const environment = {
  production: false,
  apiUrl: 'http://localhost:8095/api', // Puerto del backend en producción (cambiar por el de PROD)
  frontendUrl: 'http://localhost:4200', // Puerto del frontend en producción (cambiar por el de PROD)
};
